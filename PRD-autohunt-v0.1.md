# autohunt（自动猎手）PRD — 草案 v0.1

> 阶段：S0 需求产出。OP- 未全部关闭前不进入开发；人类确认本稿即 G0。
> 基线资料：Issue PROX-3 中的产品构思（简历管理 / Agent 自动填表+人工确认 / 投递进度监控 / 邮箱监控注册日程 / 统计）。项目仓库当前为空仓库，无既有代码基线。

## 1. 修订记录

| 版本 | 日期 | 作者 | 变更说明 |
|---|---|---|---|
| v0.1 | 2026-08-25 | ProductManager-小黑 | 首版草案，基于 Issue 初步设想结构化 |

## 2. 一句话定义

autohunt 是一个面向秋招求职者的个人工作台：集中管理简历与岗位投递，由 Agent 辅助填写网申表单（人工确认后提交），自动从邮箱识别笔试/面试安排并注册日程，并提供投递漏斗统计。

## 3. 背景与问题

秋招期间求职者（主要为学生）面临：

- 投递渠道分散（各公司官网、牛客、BOSS 直聘等），网申表单重复填写，耗时且易出错；
- 投递数量大（几十至上百），进度靠脑记或 Excel，容易漏跟进；
- 笔试/面试通知散落在邮箱，手动整理易错过时间；
- 缺乏整体视角，无法评估投递策略是否有效（哪些渠道转化高、卡在哪个环节）。

## 4. 目标与成功标准

### 4.1 产品目标（G-）

| 编号 | 目标 |
|---|---|
| G-1 | 简历一次维护、多处复用，消除重复填表 |
| G-2 | 网申填表由 Agent 代劳，人只做确认，单岗位投递耗时显著下降 |
| G-3 | 所有投递的进度集中可视、可追溯，不漏跟进 |
| G-4 | 笔试/面试安排自动从邮箱进入日程，不错过时间 |
| G-5 | 提供投递漏斗统计，支撑求职策略调整 |

### 4.2 成功指标（KPI-）

| 编号 | 指标 | 口径（分子/分母） | 目标值 | 数据源 |
|---|---|---|---|---|
| KPI-1 | Agent 填表一次成功率 | 提交成功且用户未返工修改的次数 / 发起辅助填表总次数 | ≥80%（建议值，OP-9） | 投递台账 |
| KPI-2 | 邮箱事件识别准确率 | 用户确认为"识别正确"的事件数 / 全部识别出的事件数 | ≥90%（建议值，OP-9） | 待确认事件反馈 |
| KPI-3 | 投递进度及时更新率 | 状态与实际情况一致的投递数 / 抽样投递总数 | 目标值待定（OP-9） | 台账+人工抽检 |
| KPI-4 | 确认响应时长中位数 | 从"提醒用户确认"到"用户完成确认"的时长中位数 | 目标值待定（OP-9） | 确认流日志 |

## 5. 用户角色与权限（U-）

| 编号 | 角色 | 描述 | 权限 |
|---|---|---|---|
| U-1 | 求职者（唯一角色） | 秋招应届生/在校生，产品为个人单用户工具 | 本人数据的全部读写权限 |

说明：MVP 按单用户个人工具设计，无多角色、无协作、无管理员。是否需要账号体系与多端同步见 OP-5。

## 6. 范围

### 6.1 MVP 范围内

| 模块 | 内容 |
|---|---|
| 简历管理 | 简历多版本存储、结构化档案解析与手动编辑、投递时关联简历版本 |
| Agent 辅助填表 | 在支持的站点清单内自动填充网申表单 → 预览 → 人工确认 → 提交 → 记录结果 |
| 投递台账与进度 | 岗位录入、投递记录、状态机跟踪、状态变更历史、手动更新 |
| 邮箱监控与日程 | 绑定邮箱、识别笔试/面试等事件、确认后注册日程、日程视图 |
| 统计看板 | 投递漏斗、关键指标卡、基础维度筛选 |

### 6.2 MVP 不做（非范围）

- 简历 AI 生成 / 优化建议（是否纳入后续版本见 OP-3）
- 内推、社区、面经分享等社交功能
- Offer 比较、薪资谈判辅助（OP-10）
- 多用户协作、导师/家长视角
- 无任何人工确认的静默自动投递（BR-1 明令禁止）
- 移动端原生 App（平台形态见 OP-11）

## 7. 信息架构与页面结构（建议稿，细节交 @Designer）

| 页面 | 核心内容 | 主要读者关切 |
|---|---|---|
| 工作台首页 | 待确认事项（填表确认、事件确认）、近期日程、关键指标卡 | 打开即知"今天要做什么" |
| 简历库 | 简历版本列表、结构化档案编辑 | 设计：版本对比入口；前端：上传/编辑交互 |
| 岗位看板 | 按状态分组的投递卡片、筛选/搜索 | 测试：状态流转用例的主要来源 |
| 岗位详情 | 岗位信息、投递记录、状态历史、关联日程、原始邮件回溯 | 后端：状态机与审计 |
| 日程 | 日历视图 + 列表视图，事件关联投递 | 前端：冲突展示 |
| 统计 | 漏斗图、维度筛选、指标卡 | 数据/BI：口径字典落地处 |
| 设置 | 邮箱绑定、站点清单、提醒偏好、数据管理 | 异常态：授权失效处理 |

## 8. 功能需求（FR-）

### 8.1 简历管理

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-1 | 上传/新建简历，支持多版本，可指定一份默认简历；导入格式范围见 OP-2 | P0 |
| FR-2 | 将简历解析为结构化档案（基本信息、教育经历、实习/项目经历、技能、获奖证书），字段清单见 §10；解析失败或缺失字段允许手动补全 | P0 |
| FR-3 | 每次投递记录关联所用简历版本，可回溯"投这份岗用的是哪版简历" | P0 |

### 8.2 岗位与投递台账

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-10 | 手动录入岗位：公司、岗位名称、JD 链接、工作地点、来源渠道、网申截止日期 | P0 |
| FR-11 | 生成投递记录：岗位 + 简历版本 + 投递时间 + 当前状态 | P0 |
| FR-12 | 岗位看板：按状态分组展示，支持按公司/渠道/时间筛选与关键词搜索 | P1 |

### 8.3 Agent 辅助填表

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-20 | 用户在支持站点（清单见 OP-1）发起"辅助填表"，Agent 基于结构化档案自动填充网申表单 | P0 |
| FR-21 | 填充完成后生成字段-值对照预览，并通知用户确认 | P0 |
| FR-22 | 用户可在确认前修改任意字段值 | P0 |
| FR-23 | 用户确认后提交表单；提交结果（成功/失败+失败原因）写入投递记录 | P0 |
| FR-24 | 填表失败（站点结构变化、网络异常等）时保留已填内容快照，允许用户转人工完成，并标记该站点适配失效 | P1 |

### 8.4 投递进度监控

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-30 | 按 BR-10 状态机跟踪每个投递的当前状态，支持手动更新 | P0 |
| FR-31 | 记录状态变更历史：时间、变更前后状态、来源（手动/邮箱识别/Agent） | P0 |
| FR-32 | 待办提醒：网申截止、笔试、面试前提醒；提前量与渠道见 OP-8 | P1 |

### 8.5 邮箱监控与日程

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-40 | 绑定求职邮箱；支持的邮箱类型与授权方式见 OP-4 | P0 |
| FR-41 | 监控收件箱，识别招聘相关邮件并解析事件：类型（测评/笔试/面试/offer/拒信）、时间、地点/会议链接、关联公司岗位 | P0 |
| FR-42 | 识别结果进入"待确认事件"列表；用户确认后注册到日程，可修正或丢弃误识别（策略选择见 OP-7） | P0 |
| FR-43 | 日程视图（日历+列表），事件与投递记录双向关联，可回溯原始邮件 | P0 |
| FR-44 | 邮箱授权失效时明确提示并暂停监控，不产生静默数据缺口 | P1 |

### 8.6 统计看板

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-50 | 投递漏斗：投递→笔试→面试→offer 各环节数量与转化率 | P0 |
| FR-51 | 维度筛选：按渠道、按时间段统计 | P1 |
| FR-52 | 关键指标卡：总投递数、进行中、待确认事项数、offer 数 | P0 |

## 9. 业务规则（BR-）

| 编号 | 规则 |
|---|---|
| BR-1 | 任何对外提交动作（网申表单提交）必须经用户显式确认，禁止静默自动提交。Agent 只能"填"，不能擅自"交" |
| BR-2 | 邮箱事件默认"先识别、用户确认后入日程"（建议方案 A）；若人类选择高置信度自动注册，需在 OP-7 关闭时记录决策 |
| BR-3 | 同一公司同一岗位的重复投递需提示，不强制拦截（求职场景存在主动重复投递） |
| BR-10 | 投递状态机（建议集合，OP-6 确认）：`待投递 → 已投递 → 笔试 → 面试 → offer → 已接受 / 已拒绝`；旁路终止态：`未通过（可挂在任一环节）、主动放弃、已过期`。面试轮次作为状态子属性记录（如"面试·二面"），不单独设状态 |
| BR-11 | 状态冲突时手动更新优先于邮箱自动识别；自动识别不得回退用户手动推进的状态 |
| BR-20 | 数据归属：单用户产品，全部数据仅本人可见；存储位置（本地/云端）见 OP-5 |

## 10. 字段与数据口径（字典，供后端/测试/BI 直接引用）

### 10.1 结构化简历档案字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| 姓名 | 文本 | 是 | |
| 电话 / 邮箱 | 文本 | 是 | 邮箱默认与绑定求职邮箱一致，可改 |
| 教育经历 | 列表（学校/学历/专业/起止时间/GPA） | 是 | 多条 |
| 实习/项目经历 | 列表（公司或项目名/角色/起止/描述） | 否 | |
| 技能 | 标签列表 | 否 | |
| 获奖证书 | 列表 | 否 | |
| 期望城市/岗位 | 文本 | 否 | 供 Agent 填表使用 |

### 10.2 投递记录字段

公司、岗位名、JD 链接、工作地点、来源渠道、简历版本 ID、投递时间、当前状态、状态历史列表、关联赛道备注。

### 10.3 统计口径

| 指标 | 分子 | 分母 | 备注 |
|---|---|---|---|
| 笔试转化率 | 进入"笔试"状态的投递数 | 状态为"已投递"及以后的投递数 | 口径待 OP-9 确认 |
| 面试转化率 | 进入"面试"的投递数 | 进入"笔试"的投递数 | 部分岗位无笔试环节，是否单独剔除见 OP-9 |
| offer 转化率 | 进入"offer"的投递数 | 全部已投递数 | |
| 漏斗统计范围 | — | 状态≠"待投递"的投递记录 | "待投递"（收藏未投）不计入 |

## 11. 验收标准（AC-）

| 编号 | 验收项 | 对应 |
|---|---|---|
| AC-1 | 上传一份简历后，结构化档案各必填字段正确解析或标记缺失，可手动补全保存 | FR-1/2 |
| AC-2 | 在 OP-1 确认的任一支持站点发起辅助填表，表单字段被填充且预览与档案一致；用户修改某字段后提交，站点侧提交值为修改后值 | FR-20/21/22 |
| AC-3 | 不经用户确认，系统不存在任何自动提交路径（含异常重试路径） | BR-1 |
| AC-4 | 提交成功/失败均写入投递记录，失败含原因与已填快照 | FR-23/24 |
| AC-5 | 向绑定邮箱发送一封模拟笔试通知邮件，系统在规定时间内识别出事件类型与时间，进入待确认列表；确认后出现在日程并关联对应投递 | FR-41/42/43 |
| AC-6 | 手动将投递从"已投递"推进到"面试"后，邮箱识别结果不得将其回退 | BR-11 |
| AC-7 | 统计页漏斗数字与按台账手工核算结果一致（抽样 10 条核对） | FR-50，KPI 口径 |
| AC-8 | 邮箱授权失效后，看板出现明确提示，历史数据不丢失 | FR-44 |

## 12. 空态 / 异常态 / 无权限态

| 场景 | 表现 |
|---|---|
| 空态·无简历 | 简历库引导上传；Agent 填表入口置灰并提示"先完善档案" |
| 空态·无投递 | 看板展示录入引导与示例卡片 |
| 空态·未绑定邮箱 | 邮箱监控与日程模块提示绑定；其余模块不受影响 |
| 异常·简历解析失败 | 提示失败原因，进入手动编辑模式，不阻塞其他功能 |
| 异常·Agent 填表失败 | 保留快照，引导人工完成，站点标记适配失效（FR-24） |
| 异常·邮箱授权失效 | 顶部持续提示，暂停监控，恢复后可续（FR-44） |
| 无权限态 | 单用户产品无越权场景；未绑定邮箱时的功能降级视同空态处理 |

## 13. 依赖

- 招聘站点清单确认（OP-1），Agent 适配工作量直接取决于此
- 邮箱服务商授权能力（OP-4）
- Agent/LLM 运行时与浏览器自动化方案 → 属技术选型，交 @Architect
- 本 PRD 全部 OP- 关闭（人类确认）为开发前置条件（G0 判门）

## 14. 风险（RISK-）

| 编号 | 风险 | 影响 | 缓解 |
|---|---|---|---|
| RISK-1 | 招聘站点反爬/页面结构变化导致填表失效 | Agent 填表成功率下降 | 站点适配层可插拔；失败回退人工（FR-24）；成功率监控（KPI-1） |
| RISK-2 | 简历含敏感个人信息，隐私与合规（个保法） | 法律风险 | 存储位置决策（OP-5）需含加密与删除机制；不对外共享 |
| RISK-3 | 邮箱授权与邮件内容隐私 | 用户信任 | 最小权限授权；只解析招聘相关邮件；授权随时可撤销 |
| RISK-4 | 代用户操作第三方站点账号的合规与封号风险 | 账号被封、法律灰色地带 | 人工确认提交（BR-1）；支持站点清单明示；OP-1 确认时逐站评估 |
| RISK-5 | 邮件识别误判/漏判导致错过笔试面试 | 核心承诺失效 | 确认机制（BR-2）+ 原始邮件可回溯 + 准确率监控（KPI-2） |

## 15. 待确认项（OP-）

> 以下为需人类拍板项，未关闭前不进入开发。凡 PRD 中标注"建议值/建议方案"的，均以人类确认为准。

| 编号 | 问题 | 建议方案（待确认） |
|---|---|---|
| OP-1 | MVP 支持哪些招聘站点？（公司官网网申/牛客/BOSS 直聘/猎聘/实习僧等） | 建议 MVP 限 1–2 个站点起步，优先用户实际投递量最大的渠道 |
| OP-2 | 简历导入格式范围（PDF/Word/纯文本）？是否需要在线简历编辑器？ | 建议 MVP 支持 PDF+手动编辑，不做在线编辑器 |
| OP-3 | 是否规划 AI 简历优化/生成建议（非 MVP，仅确认是否进路线图） | 建议列入 v0.2 候选 |
| OP-4 | 支持哪些邮箱（QQ/163/Gmail/Outlook）？授权方式（IMAP 授权码 / OAuth）？ | 建议 MVP 支持 IMAP 授权码通用方案 |
| OP-5 | 数据存储：本地单机 还是 云端账号体系？是否需要多设备同步？ | 建议 MVP 本地优先；若需多端则须引入账号体系，范围显著扩大 |
| OP-6 | 投递状态集合与流转规则（BR-10 建议稿）是否确认？ | 以 BR-10 建议稿为基线调整 |
| OP-7 | 邮箱事件注册策略：A) 全部先确认后入日程；B) 高置信度自动入日程+可撤销，低置信度待确认 | 建议 A（更保守，BR-2 按此撰写） |
| OP-8 | 提醒提前量（如笔试前 24h/1h）与通知渠道（应用内/邮件/其他） | 建议应用内 + 事件前 24h 与 1h 两级 |
| OP-9 | KPI 目标值与统计口径（§4.2、§10.3 建议稿）是否确认？ | 以建议稿为基线调整 |
| OP-10 | 是否需要 offer 记录/比较、薪资字段？ | 建议 offer 仅作状态与数量记录，比较功能不做 |
| OP-11 | 产品形态：桌面应用 / Web / 浏览器插件（Agent 填表通常需要插件或内置浏览器）？ | 需结合技术方案确认，建议优先听取 @Architect 意见 |
